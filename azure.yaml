# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: hilla-grocery-app
metadata:
  template: hilla-grocery-app@0.0.1-beta
services:
  web:
    project: ./src/web/hilla-grocery-app
    module: app/web
    language: java
    host: containerapp
  backend:
    project: ./src/backend/groceryservice
    module: app/backend
    language: java
    host: containerapp